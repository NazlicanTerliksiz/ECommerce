<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ECommerce" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ECommerce" parent="Base.Theme.ECommerce" />
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/darkBeige</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreenlogo</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.ECommerce</item>

    </style>

</resources>